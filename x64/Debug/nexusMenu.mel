global string $gMainWindow;

string $lsystemMenu = `menu -p $gMainWindow -label "Nexus"`;
    menuItem -label "Create Nexus Rigidbody" -command "showLSystemGUI;";
    menuItem -label "Create Nexus Cloth" -command "showLSystemGUI;";

currentUnit -time 40fps;
playbackOptions -loop "once" -maxTime 1000;

global proc showLSystemGUI()
{
}